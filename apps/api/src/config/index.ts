export const config = {
  port: process.env.PORT || 3000,
  nodeEnv: process.env.NODE_ENV || 'development',
  // Database configuration will be added here
  // RBAC configuration will be added here
  // Audit logging configuration will be added here
};

