# Application Configuration
NODE_ENV=development
PORT=3000
API_VERSION=v1

# Logging
LOG_LEVEL=info

# Security (project domain: http://164.92.71.218; production cannot use *)
CORS_ORIGIN=*
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Future: Database Configuration
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Future: Authentication & Authorization
# JWT_SECRET=your-secret-key-min-32-chars
# JWT_EXPIRES_IN=24h

# Tenant isolation (Fonte 5 - Motor Payton). Required for JWT tid/role when no user-tenant mapping.
# Must match an existing tenants.tenant_id (UUID). Login/register/refresh use this for access tokens.
# DEFAULT_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Future: RBAC Configuration
# RBAC_ENABLED=true

# Future: Audit Logging
# AUDIT_LOG_LEVEL=standard
# AUDIT_LOG_STORAGE=file|database|external
