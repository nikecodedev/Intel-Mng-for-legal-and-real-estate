# Backend API base URL (no trailing slash). Include /api/v1 if your API is versioned.
# Example: http://localhost:3000/api/v1
NEXT_PUBLIC_API_URL=

# Optional: set to true when backend sets JWT in httpOnly cookie. Then we use withCredentials and do not store tokens.
# NEXT_PUBLIC_AUTH_USE_COOKIES=false
