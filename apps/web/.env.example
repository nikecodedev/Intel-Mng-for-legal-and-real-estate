# Base URL of the API (including /api/v1). Required when the web app is served from a different origin than the API (e.g. different host or port).
# Examples:
#   Same host (reverse proxy): leave empty — the app will use same-origin + /api/v1
#   Different host: https://api.your-domain.com/api/v1
#   Local API (e.g. API on 3000, web on 3001): http://localhost:3000/api/v1
#   Project domain: http://164.92.71.218/api/v1
# NEXT_PUBLIC_API_URL=
